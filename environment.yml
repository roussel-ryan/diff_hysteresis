# conda env create -f environment.yml
name: diff_hysteresis
channels:
  - conda-forge
  - gpytorch
  - pytorch
dependencies:
  - botorch
  - gpytorch
  - ipykernel
  - ipywidgets
  - jupyterlab
  - matplotlib
  - numpy
  - pandas
  - pip
  - python=3.9
  - pytorch
  - scipy
  - pip:
    - pygmsh
